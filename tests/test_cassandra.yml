---
- provider: play_cassandra
  type: execute
  connection:
    auth_provider:
      username: user
      password: pwd
  keyspace: dev,
  query: SELECT title from play WHERE code = 1;
  assertion: result == 'hello!'
